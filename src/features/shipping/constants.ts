// Fallback usado quando um produto ainda não tem peso/dimensão cadastrados
// no admin (campos opcionais — produtos antigos ficam null até alguém
// preencher). Caixa pequena genérica, só pra cotação/etiqueta não quebrar.
export const DEFAULT_PACKAGE_WEIGHT_GRAMS = 300;
export const DEFAULT_PACKAGE_HEIGHT_CM = 4;
export const DEFAULT_PACKAGE_WIDTH_CM = 12;
export const DEFAULT_PACKAGE_LENGTH_CM = 16;
